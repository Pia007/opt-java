public class ObjectNotes {
    public static void main(String[] args) {

    }
}

// Planning your code
    // Look for objects(things)
    // Look for fields that describe each object
    // Look for actions that each object can perform

// Requirements
    // dealership(object) sells many types of cars(fields)
    // a car(objects) can be identified by its make, price, year and color (fields)
    // the app should be able to search(action) for cars based on the make and price
    // after a sale(action), the care must drive(action) to the nearest exit

// Create object
    // write the type of object
    // pass in the fields that describe it
        // Car dodge = new Car("Dodge", 11000, 2019, "blue");
            // drive()
        

